name: Enviar resumo diário Matosinhos (desativado)

on:
  workflow_dispatch:

concurrency:
  group: matosinhos-data-push
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  MAIL_TO: diogo.almeida@ulsm.min-saude.pt,marciaisabel.balazeiro@ulsm.min-saude.pt

jobs:
  send-email:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Email desativado
        run: |
          echo "Envio automático/manual de email está desativado por agora."
          echo "Código mantido em send_daily_report_email.R para reativação futura, após commit do relatório."
