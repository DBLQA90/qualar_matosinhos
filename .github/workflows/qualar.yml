name: Recolher QualAr Matosinhos

on:
  schedule:
    - cron: '0 10 * * *'
    - cron: '0 15 * * *'
  workflow_dispatch:

concurrency:
  group: qualar-push
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: 'release'

      - name: Instalar packages R
        run: |
          install.packages(c("httr", "jsonlite", "dplyr", "readr"))
        shell: Rscript {0}

      - name: Correr script
        run: Rscript fetch_qualar.R

      - name: Commit do CSV se mudou
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull origin main --rebase
          git add qualar_matosinhos.csv
          if git diff --staged --quiet; then
            echo "Sem alterações no CSV, nada a fazer."
          else
            git commit -m "Update QualAr $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            git push
          fi
